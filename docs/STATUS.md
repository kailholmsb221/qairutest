# CampusLive — статус

> Обновляется в конце каждой фазы. Продолжать сессию: «Continue CampusLive from the phase noted in docs/STATUS.md».

Последнее обновление: 2026-09-18 (вечер).

## Решения, принятые при старте (отличия от docs/ARCHITECTURE.md)

| Что | Как сделано | Почему |
|---|---|---|
| Программа помещений | **Чертежи владельца, стены дословно** (2026-09-18): `packages/map-data/plans/floor-{1,2}.svg` (Illustrator: стены — штрихи, подписи — текст) → `apps/map-editor/tools/svg2plan.py` → `tools/traced/<этаж>.json`: штрихи сохраняются как есть (`strokes`, кривые Безье не аппроксимируются) и рисуются экраном как стены; помещения между ними вычисляются (планарная сеть, мостики через проёмы, полигонизация, коридор с дырами-островами в `holes`); подпись называет грань, в которой стоит. Идентичность — `tools/floor{1,2}.ts` → `gen:floors` → `packages/map-data/vector`. Оба этажа ориентированы одинаково — дугой фасада вниз (1 этаж повёрнут на 90°: вход сверху; 2 этаж — как нарисован), поэтому в 2.5D-стопке они смотрят в одну сторону; подписи 1 этажа расставлены по фото стенда (на чертеже их нет), безымянные грани 1 этажа типизированы по цвету фото; ответвления коридора 2 этажа, отрезанные проёмами (проход к 213/209/206, полоса над WC, проход 203–204, полосы вокруг 227), перечислены в `corridor` конфигурации — иначе они серые «помещения» (точка должна лежать в самой грани с учётом её дыр) | Владелец отверг трассировку фото 2026-09-16 и принёс готовый чертёж 2 этажа (`fll2.svg`; вечером 2026-09-18 — уточнённая версия `fln22222.svg`: вторая «204» стала «WC», подписи сдвинуты) и чертёж 1 этажа (`1 fl.svg`), 1 этаж должен выглядеть как его скриншот; первоначальный поворот 2 этажа на 180° отменён 2026-09-18 по его же просьбе («этажи должны смотреть в одну сторону») |
| Редактор расписания | Админ-панель (⚙ в тикере → 📅): сетка «пары × дни недели» для выбранной аудитории, «+» в свободной ячейке добавляет шаблон занятия (дисциплина, преподаватель, группы, тип, чётность недель), ✕ удаляет. API: `GET /admin/catalog`, `GET/POST /admin/lessons`, `DELETE /admin/lessons/{id}` (X-Api-Key); сервис отклоняет двойное бронирование аудитории/преподавателя/группы (409), после изменения сбрасывает кэш каталога и рассылает новый снимок по SSE. Ключ панели берётся при сборке (`NEXT_PUBLIC_ADMIN_API_KEY`) или на лету через `/api/admin-key` (демо) | Владелец попросил «админ-панель, где можно ставить расписание». Шаблоны `lessons` — единственный источник расписания, поэтому движок/табло/карта ничего не знают о панели |
| Коды аудиторий и программа помещений | `packages/map-data/room-codes.json`: код, `schedulable`, тип, вместимость и **названия на трёх языках** (`names: {ru, kk, en}`) по программе помещений владельца (2026-09-18, вечер): **12 расписуемых** — 100 (актовый зал), 101, 200, 201 (конференц-зал), 204, 219, 222 (компьютерный класс), 223, 224, 226, 227, AI-LAB; кабинеты администрации (202 деканат, 203, 206/209 приёмные, 213 советник ректора, 214 бухгалтерия, 215, 217 маркетинг, 218 ШОП, 220/225 служебные, 221 офис регистратора), 102/102A библиотека, 103 медпункт, WC-1/2/3, CR, CINEMA, CAFE, VOID-2 — без расписания, серые («служебная зона»), но кликабельны; дубли подписей получают суффикс A (102A); остальные — `code = id` карты. `building-a.json` несёт `names`, seed пишет `rooms.name_kk/name_en` (миграция 00002), `/search` ищет по коду и всем трём названиям, интерфейс показывает название на языке экрана | Программа помещений от владельца (kk/en) 2026-09-18; кабинеты ректората и деканата не аудитории — расписание у них бессмысленно; поиск должен работать на любом языке интерфейса. Комнат 205, 207, 208 из программы на чертеже нет — не добавлены |
| Крылья | enum `west \| east \| core` вместо `north \| south \| core` | Реальный план — ландшафтный (ядра сверху, дуга фасада снизу), крылья слева/справа от центрального холла (x 700–900) |
| Источник геометрии | `vector/*.json` → `scripts/vector2map.ts` → `vector-map.json` (что рисуем) + `building-a.json` (идентичность) | SVG-трассировка не нужна: планы уже в топологической модели редактора; svg2map заменён на vector2map |
| Палитра | Токены = палитра карты (`#0d1520`, cyan `#4fd1ff`, статусы green/orange/yellow/blue), не fallback-токены промта | Один цвет — одно значение и на карте, и на табло; карта неприкосновенна |
| RoomShape | `<path data-phase>` + CSS-переходы вместо `motion.path` | 300 путей; CSS дешевле и не меняет рендер редактора |
| Zoom/pan | В фокус-виде сохранены колесо + перетаскивание из редактора | Ландшафтный план 1600×1000 с мелкими подписями |
| Зазор этажей | `max(72px, 0.46 × высота слоя)` | При 72px два больших ландшафтных этажа перекрываются; 0.46 — как на референсе владельца (нижний этаж читается целиком) |
| 2.5D-сцена по референсу владельца (2026-09-18) | На разнесённых плитах нет подписей — только точки идущих занятий; слева сверху текстовая подпись «Все этажи · разнесённый вид · нажмите на плиту…» (в фокусе — кнопка «Этаж N · … · Esc — все этажи»), справа от стопки теги-кнопки «— F2 · занято 7» на высоте своей плиты, чип «N / M аудиторий занято» с полоской; в шапке «Все» с суммой занятых и «Поиск ⌘K» текстом | Скриншот владельца: оболочка Qairumap с этими же чертежами — текстовые кнопки вместо иконок и пилюль на плитах |
| Светлая тема карты | Токены `--map-bg/--floor-fill/--slab*/--wall*/--fill-*/--plan-label/--stage-glass` переопределены в `[data-theme='light']`; статические заливки помещений заданы CSS-правилами по `data-type` (а не атрибутом fill), легенда/контролы/подписи/чипы — через токены | В светлой теме карта оставалась чёрной, а легенда и кнопки — белыми на белом («когда нажимаешь на тему, карта чёрная») |
| Порты по умолчанию | api 8090, postgres 5434, web dev 3000/3100, compose-проект `campuslive-map` | На машине разработчика 8080/5432 заняты другим стеком |
| Пакет `internal/service` | Добавлен (кэш каталога + снимка, admin use-cases) | §8.1 архитектуры описывает `service.Board`, но дерева для него не было |
| `Snapshot.announcements`, `DayTimeline.slots`, `TimeInfo.localDate/weekNumber/parity` | Добавлены в контракт | Тикер получает объявления при подключении; шкала времени знает слоты; шапка показывает неделю/чётность |
| Фокус-вид: `viewBox` = габариты всех помещений + поле (в пропорции 1600×1000), слой без CSS-scale и без `will-change` | Вместо `scale(1.12)` на композитном слое и вместо фиксированного ×1.12 | Слой с `will-change`/scale растрируется один раз и растягивается — план «плыл» при зуме; векторный zoom всегда чёткий. Чертежи заполняют viewBox почти целиком, фиксированный ×1.12 обрезал бы края здания |
| Модель плана: `holes` у помещения и `strokes` у этажа | `Room.holes: BoundaryRef[][]` (острова внутри коридора), `FloorPlan.strokes: {id, d, exterior}` (path data чертежа дословно) | Коридор вокруг островов остаётся одним помещением (заливка `evenodd`), а экран рисует кривые чертежа, а не их дискретизацию; мостики через проёмы (`virtual`) не рисуются |
| Клик по этажу в стопке = клик по любому помещению; выделение без обводки | Убраны прозрачная кнопка `floor-hit` и белый `selection-outline`/focus-stroke | Невидимая кнопка ловила фокус-рамку; выбранная аудитория лишь чуть светлее |

## Фазы

| # | Фаза | Статус | Проверки |
|---|---|---|---|
| 0 | Скелет: pnpm + Turbo, Next.js 15, Go-модуль, Compose, Caddy, CI, CLAUDE.md, openapi.yaml | ✅ | `pnpm install`, `/healthz` 200 |
| 1 | Данные: миграции goose, sqlc, `vector2map`, seed (42) | ✅ | `pnpm map:build` (103 помещения, 10 с расписанием), `pnpm seed` (40 преподавателей, 30 групп, 50 курсов, 10 слотов, 285 пар, 79 override) |
| 2 | Движок + часы | ✅ | `go test ./internal/engine` — 97.9 % покрытия, golden-фикстуры (`testdata/golden`), полный день переходов, полночь |
| 3 | REST API + контрактные тесты + ETag | ✅ | `go test ./internal/httpapi` (testcontainers + kin-openapi): каждый ответ валидируется по openapi.yaml; `board?at=` для любого времени |
| 4 | Realtime: broker, scheduler, `/events`, admin | ✅ | тест: snapshot после `POST /admin/overrides` приходит < 1 с; heartbeat; медленный клиент отбрасывается |
| 5 | Экран: layout, Header, LiveClock, Board (autofit + pager + split-flap), pills, Ticker, connection, i18n ru/kk/en | ✅ | Playwright: нет скролла на 1280×720 / 1920×1080 / 2560×1440 / 3840×2160; пагинация; SSE обновляет строки |
| 6 | Карта: Scene, FloorLayer, Slab, FloorPlan, RoomShape, exploded/focus, tooltip, RoomDetailPanel | ✅ | фокус-вид пиксельно повторяет редактор; 2.5D-стопка; e2e: фокус, клик, панель, клавиатура |
| 7 | Интерактив: SearchPalette, highlight, фильтры, TimeTravelBar, клавиатура, reduced-motion | ✅ | e2e: поиск группы подсвечивает аудиторию и фильтрует табло; travel переносит строку NEXT → NOW; reduced-motion отключает параллакс |
| 8 | Kiosk + демо: `/kiosk`, DemoAdminPanel, `pnpm demo`, `pnpm demo:script` | ✅ | e2e: киоск листает страницы и этажи; сценарий 90 с через admin API |
| 9 | Полировка: Lighthouse, a11y, пустые/after-hours/error состояния, README, деплой | 🟡 | README + `infra/deploy` (Fly.io + Vercel) готовы; состояния реализованы; **Lighthouse не прогонялся** (нет публичного URL, локально — см. «Что дальше») |

## Quality gates (локально, 2026-09-18, вечер)

- Go: `go vet ./...` ✅, `go test -count=1 ./...` ✅ (engine, realtime, httpapi интеграционные + контрактные; `TestBoard` берёт число расписуемых аудиторий из building-a.json; `TestAdmin` использует расписуемую 101 для extra-занятия). **`-race` недоступен на windows/386** — гоняется в CI (ubuntu).
- Web: `pnpm lint` ✅, `pnpm typecheck` ✅, `vitest` 47 тестов ✅, `next build` ✅.
- Playwright (CLOCK_MODE=fixed 2026-09-15 10:47): 16 сценариев ✅ (новый `admin.spec.ts`: добавить и убрать занятие в редакторе расписания) + 2 визуальных снимка (`apps/web/e2e/*.spec.ts-snapshots`, перегенерированы под чертежи).
- Геометрия: `svg2plan.py` → `gen:floors` → `check:geometry` — 0 ошибок; Σ площадей помещений = 90 % / 87 % контура (остальное — тела двойных стен чертежа, они помещениями не являются); предупреждения `gap`/`almost-axis` — стены, граничащие с телом стены, и наклонные стены чертежа, это норма.
- Артефакты: `pnpm --filter @campuslive/map-data check`, `pnpm --filter @campuslive/contracts check`, `go generate` diff — в CI.

## Известные ограничения

- Коды, расписуемость и названия (ru/kk/en) — в `room-codes.json` по программе помещений владельца; правка → `pnpm map:build && pnpm seed`. Для 215 и 227 программы нет — «Кабинет»/«Аудитория»; 205, 207, 208 на чертеже отсутствуют. Русские названия — перевод kk/en владельца; казахские приведены к нормативной орфографии (ә, ғ, қ, ң, ө, ұ, ү, і).
- Поиск по преподавателям/дисциплинам/группам — только по русским данным seed (переводов нет); по аудиториям — на всех трёх языках.
- Карта — чертежи владельца: на чертеже 1 этажа нет подписей, поэтому позиции 100–103, WC, CR, Cinema, Cafe заданы в конфигурации `svg2plan.py` (`anchors`) по фото стенда; безымянные помещения 1 этажа получили тип по цвету фото (`types`), остальные — «Помещение». Правки: геометрия — в SVG (`packages/map-data/plans/*.svg`) → `python apps/map-editor/tools/svg2plan.py` (numpy, opencv-python, shapely, svgelements, Pillow), идентичность — `tools/floor{1,2}.ts`, затем `gen:floors` → `pnpm map:build`. Точки `at` в таблицах идентичности и в конфигурации — в координатах viewBox 1600×1000; при смене поля вписывания (`MARGIN`) их нужно пересчитать (QA-рендер печатает полюс каждой грани).
- Дверные проёмы — невидимые мостики (`virtual`); двери/лестницы/лифты как отдельные символы на чертежах не обозначены (лестничные марши 1 этажа — штрихи чертежа).
- «Закрытая зона» 1 этажа и прочие тёмно-синие на фото помещения без подписи имеют `type: hall` (синяя заливка, как просил владелец); чтобы сделать их серо-синими служебными, достаточно сменить тип на `service`.
- `pnpm test` в корне падает на `@campuslive/map-data#test` (vitest без тестовых файлов) — так было и до переоцифровки; остальные пакеты зелёные.
- `golangci-lint` не установлен локально — запускается только в CI.
- Chrome performance trace 60 fps для фокуса этажа не прикладывался (нет headed-браузера в сессии); анимируются только `transform`/`opacity`, фильтры — один `drop-shadow` на группу внешних стен и один `feGaussianBlur` на контур этажа.
- Прод-сборка Next с `output: standalone` включается переменной `NEXT_STANDALONE=1` (Docker/CI): на Windows без developer mode pnpm-симлинки не создаются.

## Что дальше

1. При необходимости уточнить названия/вместимость кабинетов и расписуемость (`room-codes.json`, `tools/floor2.ts`) → `pnpm map:build` → `pnpm seed`.
2. Lighthouse: `pnpm --filter @campuslive/web build && pnpm --filter @campuslive/web start` → `npx lighthouse http://localhost:3000 --preset=desktop`.
3. Публичный демо-URL: `infra/deploy/README.md` (Fly.io + Vercel), затем вписать ссылку в README.
4. Записать 90-секундное демо: `pnpm demo` + `pnpm demo:script`.
